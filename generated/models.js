// This file is generated by Allograph. We recommend that you do not modify this file.
var knex = require('../database/connection');

export class BaseUser {
  users(args) {
    return knex('users').where(args);
  }

  createUser(args) {
    return knex.returning('id').insert({
      first_name: args.first_name,
      last_name: args.last_name,
      email: args.email,
    }).into('users').then(id => {
      return knex('users').where({ id: id[0] }).first();
    });
  }

  updateUser(args) {
    return knex('users').where({ id: args.id }).returning('id').update({
      first_name: args.first_name,
      last_name: args.last_name,
      email: args.email,
    }).then(id => {
      return knex('users').where({ id: id[0] }).first();
    });
  }

  deleteUser(args) {
    return knex('users').where({ id: args.id }).del()
  }
}

export class BaseProject {
  projects(args) {
    return knex('projects').where(args);
  }

  createProject(args) {
    return knex.returning('id').insert({
      title: args.title,
      user_id: args.user_id,
    }).into('projects').then(id => {
      return knex('projects').where({ id: id[0] }).first();
    });
  }

  updateProject(args) {
    return knex('projects').where({ id: args.id }).returning('id').update({
      title: args.title,
      user_id: args.user_id,
    }).then(id => {
      return knex('projects').where({ id: id[0] }).first();
    });
  }

  deleteProject(args) {
    return knex('projects').where({ id: args.id }).del()
  }
}

export class BaseUsers_project {
  users_projects(args) {
    return knex('users_projects').where(args);
  }

  createUsers_project(args) {
    return knex.returning('id').insert({
      users_id: args.users_id,
      projects_id: args.projects_id,
    }).into('users_projects').then(id => {
      return knex('users_projects').where({ id: id[0] }).first();
    });
  }

  updateUsers_project(args) {
    return knex('users_projects').where({ id: args.id }).returning('id').update({
      users_id: args.users_id,
      projects_id: args.projects_id,
    }).then(id => {
      return knex('users_projects').where({ id: id[0] }).first();
    });
  }

  deleteUsers_project(args) {
    return knex('users_projects').where({ id: args.id }).del()
  }
}