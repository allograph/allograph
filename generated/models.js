// This file is generated by Allograph. We recommend that you do not modify this file.
var knex = require('../database/connection');

export class BaseUser {
  users(args) {
    return knex('users').where(args);
  }

  createUser(args) {
    return knex.returning('id').insert({
      first_name: args.first_name,
      last_name: args.last_name,
      email: args.email,
    }).into('users').then(id => {
      return knex('users').where({ id: id[0] }).first();
    });
  }

  updateUser(args) {
    return knex('users').where({ id: args.id }).returning('id').update({
      first_name: args.first_name,
      last_name: args.last_name,
      email: args.email,
    }).then(id => {
      return knex('users').where({ id: id[0] }).first();
    });
  }

  deleteUser(args) {
    return knex('users').where({ id: args.id }).del()
  }
}

export class BaseTag {
  tagses(args) {
    return knex('tags').where(args);
  }

  createTag(args) {
    return knex.returning('id').insert({
      title: args.title,
    }).into('tags').then(id => {
      return knex('tags').where({ id: id[0] }).first();
    });
  }

  updateTag(args) {
    return knex('tags').where({ id: args.id }).returning('id').update({
      title: args.title,
    }).then(id => {
      return knex('tags').where({ id: id[0] }).first();
    });
  }

  deleteTag(args) {
    return knex('tags').where({ id: args.id }).del()
  }
}

export class BaseTagsProject {
  tagsProjectses(args) {
    return knex('tags_projects').where(args);
  }

  createTagsProject(args) {
    return knex.returning('id').insert({
      project_id: args.project_id,
      tag_id: args.tag_id,
    }).into('tags_projects').then(id => {
      return knex('tags_projects').where({ id: id[0] }).first();
    });
  }

  updateTagsProject(args) {
    return knex('tags_projects').where({ id: args.id }).returning('id').update({
      project_id: args.project_id,
      tag_id: args.tag_id,
    }).then(id => {
      return knex('tags_projects').where({ id: id[0] }).first();
    });
  }

  deleteTagsProject(args) {
    return knex('tags_projects').where({ id: args.id }).del()
  }
}

export class BaseProject {
  projectses(args) {
    return knex('projects').where(args);
  }

  createProject(args) {
    return knex.returning('id').insert({
      title: args.title,
      user_id: args.user_id,
    }).into('projects').then(id => {
      return knex('projects').where({ id: id[0] }).first();
    });
  }

  updateProject(args) {
    return knex('projects').where({ id: args.id }).returning('id').update({
      title: args.title,
      user_id: args.user_id,
    }).then(id => {
      return knex('projects').where({ id: id[0] }).first();
    });
  }

  deleteProject(args) {
    return knex('projects').where({ id: args.id }).del()
  }
}

export class BaseUsersProject {
  usersProjectses(args) {
    return knex('users_projects').where(args);
  }

  createUsersProject(args) {
    return knex.returning('id').insert({
      users_id: args.users_id,
      projects_id: args.projects_id,
    }).into('users_projects').then(id => {
      return knex('users_projects').where({ id: id[0] }).first();
    });
  }

  updateUsersProject(args) {
    return knex('users_projects').where({ id: args.id }).returning('id').update({
      users_id: args.users_id,
      projects_id: args.projects_id,
    }).then(id => {
      return knex('users_projects').where({ id: id[0] }).first();
    });
  }

  deleteUsersProject(args) {
    return knex('users_projects').where({ id: args.id }).del()
  }
}